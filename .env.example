# Copy this file to .env and fill in your real key. .env is gitignored --
# it will never be committed or pushed.
#
# Get your key at https://buttondown.com/keys (create one if you don't have
# one yet). It only needs permission to create/manage emails.
BUTTONDOWN_API_KEY=
